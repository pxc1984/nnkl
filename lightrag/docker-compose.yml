services:
  lightrag:
    build: .
    ports:
      - "9621:9621"
    env_file:
      - .env
    environment:
      - INPUT_DIR=/app/inputs
    volumes:
      - ./inputs:/app/inputs
      - ./rag_storage:/app/rag_storage
    restart: unless-stopped
